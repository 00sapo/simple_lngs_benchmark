sync{default.rsyncssh,
  host="lngs",
  source="./",
  targetdir="/home/fsimonet/test",
  excludeFrom=".gitignore",
  delete="running",
  delay=0,
  rsync = {
    cvs_exclude = true,
    compress = true,
    _extra = {"--max-size=15mb"},
  }
}

settings{
  nodaemon=true,
  logfile="/tmp/lsync.log"
}
